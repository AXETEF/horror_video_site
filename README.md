# 🎬 Horror Video Site

**Одностраничный хоррор-сайт с эффектом старого телевизора и автоматическим закрытием браузера.**

Этот проект создаёт жуткий пользовательский опыт в стиле фильма "Пила" с эффектами помех ТВ, красным свечением и полноэкранным видеоплеером.

## 🎯 Особенности

- 📺 **Эффект старого телевизора** - реалистичная рябь и помехи
- 🎥 **Полноэкранное видео** - видео воспроизводится во весь экран
- 🔴 **Жуткий дизайн** - тёмный фон, красные тона, пульсирующие эффекты
- ⚠️ **Предупреждающий экран** - перед входом в видео
- 🔊 **Шипящие звуки** - имитация шума телевизора
- 🚪 **Автоматическое закрытие** - браузер закрывается после видео

## 🛠️ Технологический стек

- **React 19** - фреймворк для UI
- **Tailwind CSS 4** - стилизация
- **TypeScript** - типизация
- **Vite** - сборка и разработка

## 📦 Установка

### Требования
- Node.js 18+
- npm или pnpm

### Шаги установки

```bash
# 1. Клонировать репозиторий
git clone https://github.com/YOUR_USERNAME/horror_video_site.git
cd horror_video_site

# 2. Установить зависимости
pnpm install
# или
npm install

# 3. Запустить сервер разработки
pnpm dev
# или
npm run dev

# 4. Открыть в браузере
# Перейдите на http://localhost:3000
```

## 🎮 Использование

1. **Откройте сайт** в браузере
2. **Прочитайте предупреждение** на экране
3. **Нажмите кнопку "ENTER"** для входа
4. **Подождите 2 секунды** - будут помехи ТВ
5. **Видео начнёт воспроизводиться** во весь экран
6. **После видео** снова появятся помехи
7. **Браузер закроется** автоматически

## 📹 Добавление собственного видео

Замените файл `/client/public/horror.mp4` на своё видео:

```bash
# 1. Скопируйте ваше видео в папку public
cp /path/to/your/video.mp4 client/public/horror.mp4

# 2. Перезагрузите страницу в браузере
```

Поддерживаемые форматы:
- `.mp4` (рекомендуется)
- `.webm`
- `.ogg`

## 🎨 Кастомизация

### Изменение цветов

Отредактируйте `/client/src/index.css`:

```css
:root {
  --primary: oklch(0.6 0.25 0); /* Красный цвет */
  --background: oklch(0.1 0 0); /* Чёрный фон */
}
```

### Изменение текста

Отредактируйте `/client/src/pages/Home.tsx`:

```tsx
<h1>ВАШ ТЕКСТ ЗДЕСЬ</h1>
<p>ВАШЕ ОПИСАНИЕ ЗДЕСЬ</p>
```

### Изменение времени помех

В файле `/client/src/pages/Home.tsx` измените значение в `setTimeout`:

```tsx
// Текущее значение: 2000ms (2 секунды)
setTimeout(() => {
  // Ваше значение в миллисекундах
}, 2000);
```

## 🚀 Сборка для продакшена

```bash
# Собрать оптимизированную версию
pnpm build
# или
npm run build

# Проверить результат локально
pnpm preview
# или
npm run preview
```

Собранные файлы будут в папке `dist/`.

## �배 Развёртывание

### На Vercel

```bash
# 1. Установите Vercel CLI
npm i -g vercel

# 2. Разверните проект
vercel
```

### На GitHub Pages

```bash
# 1. Измените vite.config.ts
# Добавьте: base: '/horror_video_site/'

# 2. Соберите проект
pnpm build

# 3. Создайте ветку gh-pages
git checkout --orphan gh-pages

# 4. Загрузите файлы из dist/
cp -r dist/* .

# 5. Загрузите на GitHub
git add .
git commit -m "Deploy"
git push origin gh-pages
```

### На Netlify

```bash
# 1. Соберите проект
pnpm build

# 2. Перетащите папку dist/ на netlify.com
```

## ⚙️ Скрипты

```bash
# Разработка
pnpm dev

# Сборка
pnpm build

# Предпросмотр собранного проекта
pnpm preview

# Проверка типов TypeScript
pnpm check

# Форматирование кода
pnpm format
```

## 📝 Структура проекта

```
horror_video_site/
├── client/
│   ├── public/
│   │   └── horror.mp4          # Видеофайл
│   ├── src/
│   │   ├── pages/
│   │   │   ├── Home.tsx        # Главная страница
│   │   │   └── NotFound.tsx
│   │   ├── components/         # React компоненты
│   │   ├── App.tsx             # Главное приложение
│   │   ├── main.tsx            # Точка входа
│   │   └── index.css           # Глобальные стили
│   └── index.html              # HTML шаблон
├── server/
│   └── index.ts                # Express сервер
├── package.json
└── README.md
```

## 🐛 Решение проблем

### Видео не воспроизводится
- Проверьте, что файл `horror.mp4` находится в папке `/client/public/`
- Убедитесь, что формат видео поддерживается браузером
- Проверьте консоль браузера на ошибки

### Браузер не закрывается
- Это нормально - браузеры блокируют `window.close()` в некоторых ситуациях
- Пользователь может закрыть вкладку вручную

### Помехи ТВ не видны
- Проверьте, что CSS анимации включены
- Очистите кэш браузера (Ctrl+Shift+Delete)

## 📄 Лицензия

MIT License - см. файл LICENSE

## 👨‍💻 Автор

Создано с помощью Manus AI

## 🤝 Вклад

Если вы хотите улучшить проект:

1. Форкните репозиторий
2. Создайте ветку для вашей функции (`git checkout -b feature/amazing-feature`)
3. Коммитьте изменения (`git commit -m 'Add amazing feature'`)
4. Запушьте в ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📞 Поддержка

Если у вас есть вопросы или проблемы, откройте Issue на GitHub.

---

**Наслаждайтесь жутким опытом! 👻**
